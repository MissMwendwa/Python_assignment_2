A list is a data structure in python that stores elements that can be repeated while a dictionary is a data structure that stores data in key value pairs that does not supprt repetion of elements.
when you care about order, therefore, you must use list if your choice of containers is limited to these three.
dict associates each key with a value, while list and set just contain values